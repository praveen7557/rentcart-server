const { server, options } = require('../../server');
const mongoose = require('mongoose');


module.exports = async () => {
  // global.httpServer = await server.start(options, ({ port }) => console.log(`Server is running at: ${port}`));
}